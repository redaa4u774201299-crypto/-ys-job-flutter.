# تحقق GitHub Pages — 2026-08-21

تمت زيارة النسخة المنشورة على الرابط `https://redaa4u774201299-crypto.github.io/-ys-job-flutter./` بعد نجاح مسار النشر للالتزام `79065d6`.

ظهرت واجهة YS.JOB العربية واتجاه RTL وشريط التنقل ونموذج البحث بصورة صحيحة. عند اكتمال الانتظار، ظهرت في قسم «أحدث الوظائف» رسالة «تعذر تحميل أحدث الوظائف حالياً». لم تُنشأ أي بيانات اختبار أو بيانات وهمية للتحقق من ذلك. يلزم فصل سبب هذه الحالة بين عدم وجود وثائق وظائف نشطة عامة وبين إعدادات قراءة Firestore المنشورة عند اختبار Firebase الحقيقي بحساب مصرح به.

اختُبر الرابط المباشر `https://redaa4u774201299-crypto.github.io/-ys-job-flutter./job-details/nonexistent-public-check`، فأعاد GitHub Pages صفحة 404 الخاصة به بدل تشغيل تطبيق Flutter. هذا يثبت أن مسار التطبيق صحيح داخليًا، لكن حزمة Pages لا تتضمن بعد بديل `404.html` لتوجيه روابط المسارات الأمامية إلى ملف Flutter `index.html`.

عولج السبب في مسار CI بإضافة خطوة تنسخ `build/web/index.html` إلى `build/web/404.html` قبل رفع artifact الخاص بـ Pages. تحقق البناء المحلي من أن الملفين متطابقان ومن احتفاظ البديل بالمسار الأساسي `/-ys-job-flutter./`. يلزم تأكيد النتيجة على الرابط العام بعد دفع هذا الإصلاح واكتمال النشر.

بعد نشر الالتزام `dff7abb` ونجاح مسار GitHub Actions، أعيد فتح الرابط نفسه. حُمّل تطبيق YS.JOB وبقي عنوان المتصفح على مسار التفاصيل، ثم ظهرت رسالة التطبيق «هذه الوظيفة غير متاحة حالياً» للمعرّف غير الموجود. بذلك تأكدت إعادة كتابة المسار إلى Flutter ونجاح جلب التفاصيل البديل عند فتح رابط مباشر، من دون إدخال أي بيانات اختبار إلى Firestore.
