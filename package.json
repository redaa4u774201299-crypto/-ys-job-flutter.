{
  "name": "ys-job-flutter-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-ysjob --only firestore \"vitest run test/firestore_rules\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0",
    "vitest": "^3.0.0"
  }
}
